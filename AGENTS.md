# SandPlanet Agent Instructions

## Source of truth
- 작업을 시작하기 전에 `docs/DESIGN_STATE.md`, `docs/PROTOTYPE_SCOPE.md`, `docs/UNITY_ARCHITECTURE.md`를 읽는다.
- 기획상 **확정**과 **가안**을 구분한다. 가안을 임의로 확정하지 않는다.
- 현재 프로젝트의 우선순위는 완성형 21일 게임을 한 번에 만드는 것이 아니라, 최종 게임으로 확장 가능한 Unity 3D 기반 프로토타입에서 핵심 루프를 검증하는 것이다.

## Scope control
- 요청받지 않은 시스템, NPC, 퀘스트, 자원, UI를 새로 추가하지 않는다.
- 새 기능은 기존 `시간 / 의지 / 개인·대인·기술 / 호감도 / 설득 퀘스트 / 수송선` 구조 안에서 먼저 해결한다.
- 구현 때문에 기획을 바꿔야 한다면 코드에서 임의 변경하지 말고 변경 제안을 먼저 남긴다.
- TBD는 오류가 아니다. 결정되지 않은 사양은 임시값임을 명확히 표시하고 정식 사양처럼 굳히지 않는다.

## Unity 3D principles
- 엔진은 **Unity 3D**다. 정확한 에디터 버전과 렌더 파이프라인은 아직 TBD다.
- 버리는 프로토타입보다 최종 게임으로 확장 가능한 기반 코드를 우선한다.
- 정적 콘텐츠 정의와 런타임 상태를 분리한다.
- NPC, 인카운터, 스킬, 수송선 시설은 가능한 한 데이터 추가로 확장할 수 있게 만든다.
- 한 NPC/퀘스트 전용 로직을 핵심 매니저에 하드코딩하지 않는다.
- 3D 공간은 그레이박스로 시작하며, 아트 완성도보다 상호작용과 상태 변화 검증을 우선한다.
- 카메라/이동 방식은 아직 확정하지 않았으므로 시스템 코드와 느슨하게 결합한다.

## Prototype principles
- 수치와 데이터는 하드코딩보다 조정 가능한 데이터 구조로 분리한다.
- 프로토타입에서는 아트 완성도보다 시스템 상태와 선택 결과가 명확히 보이는 것을 우선한다.
- 하나의 기능을 만들 때 입력 → 비용 → 상태 변화 → 피드백까지 최소한의 완결된 루프로 구현한다.
- 플레이어가 하루를 어떻게 사용했는지 추적할 수 있어야 한다.

## Current core rules
- 게임 기간: 21일.
- 기본 행동 시간: 08:00~22:00.
- 기본 최대 의지력: 5.
- 하루 종료 후 수면 회복: +2 의지.
- 낮잠: 2시간 소비, +1 의지.
- 성장 스탯: 개인 / 대인 / 기술, 각 최대 20.
- 시작 시 세 스탯에 총 6포인트 배분.
- 하루 종료 시 그날의 경향성에 따라 세 스탯 중 하나가 +1.
- 호감도: NPC별 0~5.
- 설득은 게이지 전투가 아니라 퀘스트의 특정 설득 기회와 조건으로 처리한다.

## Coding workflow
- 변경 전에 관련 문서를 확인한다.
- 가능한 경우 작은 단위로 구현하고 테스트한다.
- 작업 완료 후 변경한 시스템과 남은 가정을 요약한다.
